import React from 'react';
import { View, Text } from 'react-native';

import styles from './_temp.styles';

const Template = ({}) => {
  return (
    <View style={styles.screen}>
      <Text />
    </View>
  );
};

export default Template;
